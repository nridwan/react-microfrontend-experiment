---
description: app application component.
labels: ['app', 'app']
---

Configure the app plugin on your workspace:

```json
"nridwan.sample-apps/main/app": {},
```

### Run the application

You can run your application on a separate port to see it outside of the Bit workspace

```bash
bit run app
```
