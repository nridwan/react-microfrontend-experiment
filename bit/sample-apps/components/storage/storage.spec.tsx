import React from 'react';
import { render } from '@testing-library/react';
import { BasicStorage } from './storage.composition';

it('should render with the correct text', () => {
  expect(true).toBeTruthy();
});
